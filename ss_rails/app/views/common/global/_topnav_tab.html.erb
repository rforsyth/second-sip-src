<%-
  link = case topnav_tab.tab
  when :home then
    link_to('Home', "/", :class => topnav_tab.css_class)
  when :metadata then
    link_to('Metadata', lookups_path, :class => topnav_tab.css_class)
  when :users then
    link_to('Users', tasters_path, :class => topnav_tab.css_class)
  when :beverage then
    link_to(@note_class.name.titleize.pluralize,
            browse_notes_path(@note_class.name.tableize), :class => topnav_tab.css_class)
  when :user then
    link_to(displayed_taster.username, taster_path(displayed_taster), :class => topnav_tab.css_class)
  when :my_notes then
    link_to("My Notes", polymorphic_path([current_taster, @note_class]), :class => topnav_tab.css_class)
  when :reference then
    link_to('References', reference_lookups_path, :class => topnav_tab.css_class)
  else raise 'Unexpected tab: ' + topnav_tab.tab.to_s
  end
-%>
<li><%= link %></li>