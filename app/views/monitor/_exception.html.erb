<div class="list_item">
  <span class="item_name">
    <%= link_to(format_exception_title(exception),
                exception_path(:id => exception.id)) %></span>
  <%= " (#{format_short_date(exception.created_at)})" %>
  <p><%= truncate(exception.message, :length => 150) %></p>
</div>