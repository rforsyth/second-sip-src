<div class="list_item">
  <span class="item_name">
    <%= link_to "#{reference_product.reference_producer.name} #{reference_product.name}", reference_product %>
  </span>
  <p><%= truncate(reference_product.description, :length => 150) %></p>
</div>